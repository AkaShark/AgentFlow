# Postgres
DATABASE_URL="postgresql://agentflow:agentflow@localhost:5432/agentflow?schema=public"

# Server
PORT=4000
HOST=0.0.0.0

# Where the hook forwarder writes JSONL events
AGENTFLOW_EVENTS_DIR=/data/events
